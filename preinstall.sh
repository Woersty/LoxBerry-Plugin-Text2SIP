#!/usr/bin/env bash

# Text2SIP pre-install hook.
# Executed by LoxBerry as user "loxberry" before the plugin files are installed.
# No pre-install migration is required; privileged setup is handled in postroot.sh.

exit 0
